import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amanda.checklist',
  appName: 'checklist',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
